webdriver {
  driver = provided
  provided {
    type = "healenium"
    healenium = "com.company.saucedemo.drivers.SelfHealingDriverSource"
  }
}

serenity {
  project.name    = "SauceDemo Self-Healing con Healenium"
  # Captura screenshot después de cada paso de Screenplay
  take.screenshots = AFTER_EACH_STEP
  maintain.session = false
}